# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm ci && npm run build:prod"
  publish = "dist/jj"

# Angular Router (rotas como /xulia) — fallback para index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
